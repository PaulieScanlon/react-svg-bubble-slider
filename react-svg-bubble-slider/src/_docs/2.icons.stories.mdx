import { Meta } from '@storybook/addon-docs/blocks'

import { SvgIcon } from '../components/SvgIcon'
import { iconPaths } from '../components/SvgBubbleSlider/iconPaths'

<Meta title="Icons" />

# Icons

Here's a list of all the icons you can use.

To provide a restricted array of icons for your SvgBubbleSlider see [icons](/?path=/docs/svgbubbleslider--icons) in the Props menu

<Flex
  sx={{
    flexWrap: 'wrap',
  }}
>
  {iconPaths.map((icon, index) => {
    const { name, path } = icon
    return (
      <Flex
        key={index}
        sx={{
          alignItems: 'center',
          boxShadow: 0,
          flexDirection: 'column',
          justifyContent: 'space-between',
          px: 4,
          py: 3,
          m: 2,
          width: ['46%', '28%'],
        }}
      >
        <SvgIcon name={name} size={64} />
        <Text
          sx={{
            color: 'darkGray',
            mt: 3,
            textAlign: 'center',
          }}
        >
          {name}
        </Text>
      </Flex>
    )
  })}
</Flex>
